# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# A disused git directory left behind when the repository moved into frontend/.
# It was tracked — 273 files of object store, hooks samples and refs committed
# into the working repo. Untracked 3 Sep 2026; ignored so it cannot return.
.git_disabled/

# The Claude Code preview's dev-server config. Local tooling, not project code.
.claude/
